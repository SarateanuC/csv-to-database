package com.example.csvtodatabase.service;

import lombok.SneakyThrows;
import org.springframework.stereotype.Service;

@Service
public class CsvService {
    @SneakyThrows
    public void csvToEntity(String path) {
        // List<User> users = new ArrayList<>();

//        try (LineIterator lineIterator = lineIterator(new File(path), "UTF-8")) {
//            while (lineIterator.hasNext()) {
//                lineIterator.nextLine().;
//            }
//        try (CSVReader csvReader = new CSVReader(new FileReader(new File(path)))) {
//            List<String[]> r = csvReader.readAll();
//            csvReader.readAll().stream().map(e->e.)
//            r.forEach(x -> System.out.println(Arrays.toString(x)));
//        }
    }

}

