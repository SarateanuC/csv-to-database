databaseChangeLog:
  - changeSet:
      - id: createUsersCsv.yml
        author: carolina.sarateanu
        failOnError: true
        changes:
      - sql:
          dbms: 'postgresql'
          splitStatements: false
          sql: create table user_xml(       
            ИдДок uuid,
            ДатаСост varchar(500),
            ДатаВклМСП varchar(500),
            ВидСубМСП varchar(500),
            КатСубМСП varchar(500),
            ПризНовМСП varchar(500),
            СведСоцПред varchar(500),
            Фамилия varchar(500),
            Имя varchar(500),
            Отчество varchar(500),
            КодРегион varchar(500),
            ТипРегион varchar(500),
            НаимРегион varchar(500),
            ТипГОРОД varchar(500),
            НаимГОРОД varchar(500),
            СвОКВЭД varchar(50000)
            );